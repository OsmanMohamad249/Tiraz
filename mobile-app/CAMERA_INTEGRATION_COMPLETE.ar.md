# دليل التكامل النهائي - معالجة إطارات الكاميرا

## ✅ ما تم إنجازه

### 1. تحديث `VisionService.dart`

تمت إضافة الميزات التالية:

#### **طريقة `processFrameFromCamera`**
```dart
Future<void> processFrameFromCamera(
  CameraImage image, {
  int rotation = 0,
  int frameSkip = 2, // معالجة كل إطار ثاني (~15 إطار/ثانية)
})
```

**تحسينات الأداء:**
- ✅ تخطي الإطارات إذا كان النظام لا يزال يعالج الإطار السابق
- ✅ معالجة كل إطار Nth (قابل للتعديل: 1 = 30fps، 2 = 15fps)
- ✅ إرسال بيانات YUV الخام مباشرة للطبقة الأصلية (سريع في Dart)
- ✅ تحويل YUV إلى Bitmap يحدث على GPU في Android

#### **إحصائيات الأداء**
```dart
// إحصائيات مراقبة الأداء
VisionService.instance.isProcessing      // هل يتم المعالجة حالياً؟
VisionService.instance.totalFrames       // إجمالي الإطارات المستلمة
VisionService.instance.processedFrames   // الإطارات المعالجة فعلياً
VisionService.instance.effectiveFps      // FPS الفعلي
VisionService.instance.resetCounters()   // إعادة تعيين العدادات
```

### 2. تحديث `PoseDetectorHelper.kt`

تمت إضافة دعم كامل لبيانات YUV:

#### **طريقة `convertYuv420ToBitmap`**
```kotlin
private fun convertYuv420ToBitmap(yuvData: ByteArray, width: Int, height: Int): Bitmap?
```

**الوظيفة:**
- تحويل بيانات YUV420 الخام من Flutter إلى Bitmap
- استخدام `YuvImage` و `compressToJpeg` للكفاءة
- دعم تلقائي للصيغ المشفرة (JPEG/PNG) والخام (YUV)

#### **طريقة `rotateBitmap`**
```kotlin
private fun rotateBitmap(source: Bitmap, degrees: Float): Bitmap
```

**الوظيفة:**
- تدوير الصورة حسب اتجاه الكاميرا
- ضروري للكاميرا الأمامية/الخلفية

## 📱 كيفية الاستخدام الكامل

### الخطوة 1: تهيئة MediaPipe
```dart
await VisionService.instance.initialize();
```

### الخطوة 2: إعداد الكاميرا
```dart
final cameras = await availableCameras();
final camera = cameras.first;

_cameraController = CameraController(
  camera,
  ResolutionPreset.medium,
  enableAudio: false,
  imageFormatGroup: ImageFormatGroup.yuv420, // مهم لـ Android
);

await _cameraController!.initialize();
```

### الخطوة 3: بدء معالجة الإطارات
```dart
_cameraController!.startImageStream((CameraImage image) {
  VisionService.instance.processFrameFromCamera(
    image,
    rotation: 0,      // 0, 90, 180, 270 حسب اتجاه الكاميرا
    frameSkip: 2,     // معالجة كل إطار ثاني
  );
});
```

### الخطوة 4: الاستماع للنتائج
```dart
VisionService.instance.poseStream.listen((landmarks) {
  // landmarks تحتوي على 33 نقطة مفصلية
  print('تم اكتشاف ${landmarks.length} نقطة');
  
  for (var landmark in landmarks) {
    print('x: ${landmark.x}, y: ${landmark.y}, z: ${landmark.z}');
    print('ثقة: ${landmark.visibility}');
  }
});
```

### الخطوة 5: التنظيف
```dart
await _cameraController?.dispose();
await VisionService.instance.dispose();
```

## 🎯 تدفق البيانات الكامل

```
┌─────────────────────────────────────────────┐
│  Flutter Camera                             │
│  - ImageFormatGroup.yuv420                  │
│  - 30 إطار/ثانية                           │
└────────────┬────────────────────────────────┘
             │
             │ CameraImage (YUV420)
             ↓
┌─────────────────────────────────────────────┐
│  VisionService.processFrameFromCamera()     │
│  - تخطي الإطارات (frameSkip)               │
│  - تحويل YUV إلى Uint8List                 │
│  - _concatenatePlanes()                     │
└────────────┬────────────────────────────────┘
             │
             │ MethodChannel.invokeMethod('processFrame')
             │ {imageData, width, height, rotation}
             ↓
┌─────────────────────────────────────────────┐
│  MainActivity.processFrame() [Android]      │
│  - استخراج البيانات من MethodCall          │
│  - استدعاء PoseDetectorHelper              │
└────────────┬────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────┐
│  PoseDetectorHelper.detectFromBytes()       │
│  - convertYuv420ToBitmap()                  │
│  - rotateBitmap() إذا لزم الأمر            │
│  - BitmapImageBuilder → MPImage             │
│  - poseLandmarker.detectAsync()             │
└────────────┬────────────────────────────────┘
             │
             │ MediaPipe Processing (GPU)
             ↓
┌─────────────────────────────────────────────┐
│  PoseDetectorHelper.handlePoseResult()      │
│  - استخراج 33 نقطة مفصلية                 │
│  - تحويل إلى List<Map<String, Double>>     │
│  - onResults() callback                     │
└────────────┬────────────────────────────────┘
             │
             │ EventChannel.EventSink.success()
             │ List<Map> مع x, y, z, visibility
             ↓
┌─────────────────────────────────────────────┐
│  VisionService.poseStream                   │
│  - تحويل إلى List<PoseLandmark>            │
│  - بث للمستمعين في Flutter                │
└─────────────────────────────────────────────┘
             │
             ↓
┌─────────────────────────────────────────────┐
│  UI Layer (Flutter Widgets)                 │
│  - رسم الهيكل العظمي                       │
│  - عرض الإحصائيات                          │
│  - استخدام البيانات للقياسات               │
└─────────────────────────────────────────────┘
```

## ⚡ تحسينات الأداء المطبقة

### في Dart (Flutter)
1. ✅ **تخطي الإطارات**: `if (_isProcessing) return;`
2. ✅ **FPS Limiter**: `_frameCount % frameSkip`
3. ✅ **تسلسل سريع للـ Planes**: `_concatenatePlanes()`
4. ✅ **عدم تحويل YUV في Dart**: إرسال البيانات الخام

### في Kotlin (Android)
1. ✅ **معالجة غير متزامنة**: `executor?.execute { ... }`
2. ✅ **تسريع GPU**: `Delegate.GPU`
3. ✅ **تحويل YUV أصلي**: `YuvImage.compressToJpeg()`
4. ✅ **تحرير الذاكرة**: `bitmap.recycle()`
5. ✅ **Timestamp متزايد**: منع النتائج القديمة

## 📊 مراقبة الأداء

### كود المثال
```dart
// عرض FPS في الواجهة
Text('FPS: ${VisionService.instance.effectiveFps.toStringAsFixed(1)}')

// عرض عدد الإطارات
Text('${VisionService.instance.processedFrames}/${VisionService.instance.totalFrames}')

// طباعة إحصائيات كل 30 إطار (مدمجة في VisionService)
// 📊 Processed 30 frames (Effective FPS: ~14.5)
```

## 🔧 إعدادات قابلة للتعديل

### التحكم في FPS
```dart
// أداء عالي (30 FPS) - يستهلك بطارية أكثر
VisionService.instance.processFrameFromCamera(image, frameSkip: 1);

// متوازن (15 FPS) - موصى به
VisionService.instance.processFrameFromCamera(image, frameSkip: 2);

// توفير الطاقة (10 FPS)
VisionService.instance.processFrameFromCamera(image, frameSkip: 3);
```

### دقة الكاميرا
```dart
// دقة عالية - أدق لكن أبطأ
ResolutionPreset.high

// متوسطة - متوازن (موصى به)
ResolutionPreset.medium

// منخفضة - أسرع لكن أقل دقة
ResolutionPreset.low
```

## 🐛 استكشاف الأخطاء

### لا توجد نتائج من الكاميرا
```dart
// تأكد من تهيئة VisionService أولاً
await VisionService.instance.initialize();

// تأكد من تنسيق YUV
imageFormatGroup: ImageFormatGroup.yuv420

// تحقق من السجلات
// 📊 Processed X frames (Effective FPS: ~Y)
```

### FPS منخفض جداً
```dart
// قلل frameSkip
frameSkip: 1  // بدلاً من 2

// استخدم دقة أقل
ResolutionPreset.low

// تحقق من تفعيل GPU
// في PoseDetectorHelper: Delegate.GPU ✓
```

### استهلاك ذاكرة عالي
```dart
// تأكد من استدعاء dispose
await VisionService.instance.dispose();

// زيادة frameSkip
frameSkip: 3  // معالجة أقل

// تحقق من تحرير Bitmap في Android
// bitmap.recycle() موجود في الكود ✓
```

## 📝 ملف كامل للمثال

راجع الملف: `lib/examples/pose_detection_camera.dart`

يحتوي على:
- ✅ إعداد كامل للكاميرا
- ✅ تكامل VisionService
- ✅ رسم الهيكل العظمي على المعاينة
- ✅ عرض إحصائيات الأداء
- ✅ معالجة الأخطاء

## 🎉 الحالة النهائية

| المكون | الحالة |
|--------|--------|
| VisionService.dart | ✅ جاهز |
| PoseDetectorHelper.kt | ✅ جاهز |
| MainActivity.kt | ✅ جاهز |
| دعم YUV420 | ✅ جاهز |
| إحصائيات الأداء | ✅ جاهز |
| مثال كامل | ✅ جاهز |
| التوثيق | ✅ جاهز |

---

**التكامل مكتمل بنسبة 100%!** 🚀

يمكنك الآن تشغيل التطبيق واستخدام الكشف عن الوضعية في الوقت الفعلي.
