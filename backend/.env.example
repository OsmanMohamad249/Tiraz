# Environment Configuration
# Determines the application environment (development, staging, or production)
# Default: development
# Production: Automatically disables DEBUG mode and enforces strict security
ENVIRONMENT=development

# Database Configuration
# REQUIRED: PostgreSQL database connection URL
# Format: postgresql://username:password@host:port/database_name
# Example: postgresql://taarez_user:secure_password@localhost:5432/taarez_db
DATABASE_URL=postgresql://taarez:your_password_here@postgres:5432/taarez_db

# Security Configuration
# REQUIRED: Secret key for JWT token signing
# MUST be a strong, random string of at least 32 characters
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# NEVER commit the actual secret key to version control
SECRET_KEY=your-strong-secret-key-at-least-32-chars-long-change-this

# JWT Configuration
# Algorithm for JWT encoding (default: HS256)
ALGORITHM=HS256

# Access token expiration in minutes (default: 30)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API Configuration
# API version 1 prefix (default: /api/v1)
API_V1_PREFIX=/api/v1

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# In development: http://localhost:3000,http://localhost:8080
# In production: https://your-domain.com,https://app.your-domain.com
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Debug Mode
# Set to false in production (automatically false when ENVIRONMENT=production)
# Default: true for development/staging, false for production
DEBUG=true

# AI Service Configuration
# URL for the AI model service (optional, default: http://ai-models:8000)
AI_SERVICE_URL=http://ai-models:8000
